#ifndef __CONSTANTS__
#define __CONSTANTS__

const float ANALOG_WRITE_SCALE = 255.0;
const float ANALOG_READ_SCALE = 1024.0; // 10 bits
const float SERIAL_WRITE_SCALE = 65536.0; // 16 bits...just fill out 2 bytes
const float DEG_PER_REV = 360.0;

#endif